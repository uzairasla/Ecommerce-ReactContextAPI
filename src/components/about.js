import React from "react";

const about = () => {
  return (
    <div className="container">
      <p className="lead">
        This project was done to demonstrate the ability to use Context API and
        react. Most of the styling done using Bootstrap.
      </p>
    </div>
  );
};
export default about;
